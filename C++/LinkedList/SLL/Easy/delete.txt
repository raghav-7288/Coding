    void deleteNode(ListNode* node) {
        ListNode *temp=node->next;
        swap(node->val,temp->val);
        node->next=temp->next;
    }

----------------------------------------------------------

    //TODO other method