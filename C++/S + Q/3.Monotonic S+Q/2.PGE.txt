vector<int> prevGreater(vector<int> &A) {
    int n=A.size();
    vector<int> pge(n);
    stack<int> st;
    for(int i=0;i<n;i++){
        while(!st.empty() && st.top() <= A[i]) st.pop();
        if(st.empty()) pge[i]=-1;
        else pge[i]=st.top();
        st.push(A[i]);
    }
    return pge;
}